# WeibanPro 微伴助手

基于 Python 和 PyQt5 开发的微伴网课平台智能辅助工具，支持全自动刷课、智能题库答题、验证码自动识别等功能。

## 注意事项

仅供学习参考，不能用于非法用途，请在下载后 24 小时内删除。使用本工具产生的任何后果由使用者自行承担。

## 使用方法

### 前提条件

1. Python 3.8+（推荐 3.11-3.12 版本）
2. 安装时勾选 "Add Python to PATH" 选项
3. 良好的网络环境

### 安装依赖

1. 进入项目文件夹
2. Shift+右键，选择 "PowerShell 打开" 或 "终端打开"
3. 执行以下命令（一行一行输入并回车）：

```shell
# 设置国内镜像源（可选，加速安装）
pip config set global.index-url https://mirrors.aliyun.com/pypi/simple
pip config set install.trusted-host mirrors.aliyun.com

# 安装依赖
pip install -r requirements.txt
```

### 运行工具

在命令窗口输入以下命令并回车：

```shell
python mian.py
```

### 使用步骤

1. **登录设置**
   - 在左侧面板输入微伴账号、密码和完整的学校中文名称
   - 点击 "登录获取课程" 按钮

2. **任务配置**
   - 从下拉菜单选择要学习的课程
   - 设置考试时长（建议 240-300 秒）
   - 设置允许错题数量（建议 3-5 题）

3. **开始运行**
   - 点击 "开始任务" 按钮
   - 程序将自动完成学习任务，右侧可查看实时进度

### 注意事项

- 运行时请不要关闭程序窗口，否则任务会终止
- 考试时会有等待时间，请耐心等待
- AI 功能需要额外配置（见 `ai.conf` 文件或程序内 AI 配置）

## 核心功能

- ✅ 自动登录（支持验证码自动识别）
- ✅ 全自动刷课（推送课、自选课、必修课）
- ✅ 智能题库答题（内置海量题库 + AI 辅助）
- ✅ 现代化 UI 界面
- ✅ 实时进度监控

## 常见问题

**Q: 登录失败怎么办？**
A: 检查账号密码是否正确，确保学校名称输入完整（不要用简称）。

**Q: 题库里没有我的题？**
A: 程序会自动尝试使用 AI 或随机策略答题，并收集新题目到本地题库。

**Q: 验证码识别失败？**
A: 取消勾选 "自动识别验证码"，手动输入验证码。

**Q: 程序运行报错？**
A: 确保 Python 版本为 3.8+，尝试重新安装依赖：
```shell
pip install -r requirements.txt --force-reinstall
```

## AI 配置（可选）

1. 运行程序，点击 "AI 配置" 按钮
2. 填写 AI 服务提供商的 API 地址、API 密钥和模型名称
3. 支持 OpenAI、DeepSeek、智谱 AI 等 OpenAI 兼容接口

## 项目结构

```text
WeibanPro/
├── mian.py              # 主程序入口
├── WBCore.py            # 核心业务逻辑
├── QuestionBank/        # 题库文件夹
│   └── result.json      # 题库数据
├── ai.conf              # AI 配置文件
└── requirements.txt     # 依赖清单
```

## 贡献者

感谢所有为本项目做出贡献的开发者！

---

如果觉得好用，请给个 Star ⭐️ 支持一下！
